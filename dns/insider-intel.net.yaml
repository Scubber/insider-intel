# insider-intel.net — future PRODUCTION domain, currently in its aging
# period. The parking redirect (301 → intel.thederpweb.com, apex + www)
# is a WORKER, not DNS: the `park-redirect` Workers script is attached to
# both hostnames as Workers Custom Domains (installed + verified by the
# dns-redirect workflow, 2026-08-11).
#
# IMPORTANT: Workers Custom Domains create and own their DNS records.
# This file must stay EMPTY of apex/www entries while the Worker is
# attached — declaring them here would make octoDNS fight the Worker for
# the records and break the redirect.
#
# At cutover (~3-6 months): detach the custom domains / delete the
# park-redirect Worker, then land the real records here — apex/www to
# GitHub Pages (or Cloudflare Pages), api → ghs.googlehosted.com.
{}
